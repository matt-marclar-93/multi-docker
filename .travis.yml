sudo: required
language: generic 
services:
  - docker

before_install:
  - docker build -t cincobot/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true cincobot/react-test npm test

after_success:
  - docker build -t cincobot/multi-client ./client
  - docker build -t cincobot/multi-nginx ./nginx
  - docker build -t cincobot/multi-server ./server
  - docker build -t cincobot/multi-worker ./worker
  # Login to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push cincobot/multi-client
  - docker push cincobot/multi-nginx
  - docker push cincobot/multi-server
  - docker push cincobot/multi-worker

deploy:
  provider: elasticbeanstalk
  region: 'us-east-1'
  app: 'multi-docker'
  env: 'MultiDocker-env'
  bucket_name: 'elasticbeanstalk-us-east-1-978616807100'
  bucket_path: 'docker-multi'
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY

